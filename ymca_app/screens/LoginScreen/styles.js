import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  loginField: {
    width: '60%',
    height: 50,
    borderBottomWidth: 2,
    borderBottomColor: 'gray',
    marginTop: '2.5%',
    marginBottom: '2.5%',
  },

  logo: {
    transform: [{scale: 0.6}],
    marginTop: '10%'
  },
});
